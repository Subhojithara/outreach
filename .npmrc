# Disable binary downloads for lightningcss to use JS implementation
lightningcss_binary_host_mirror=none

# Disable telemetry
next_telemetry_disabled=1

# Force using JS implementations instead of native binaries
node_options=--no-node-snapshot

# Skip optional dependencies that might cause issues
optional=false

# Increase network timeout for potentially slow connections
network-timeout=100000